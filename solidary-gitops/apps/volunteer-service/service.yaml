apiVersion: v1
kind: Service
metadata:
  name: volunteer-service
  namespace: solidary
spec:
  selector:
    app: volunteer-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP